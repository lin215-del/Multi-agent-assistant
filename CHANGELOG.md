# 更新日志

本项目采用语义化版本号。重要修改前请先创建本地备份或提交到 Git。

## [1.0.0] - 2026-07-23

### FastAPI 单一架构

- 完整删除 Next.js、TypeScript、pnpm 和 Vercel 运行文件。
- FastAPI 成为唯一正式 Web 入口，并提供一键创建虚拟环境和启动脚本。
- Python Agent 链路改为真实 LangGraph `StateGraph` 条件编排。
- 新增首次管理员初始化、普通用户注册、PBKDF2 密码哈希、会话过期和管理员/普通用户权限隔离。
- 新增管理员用户管理、持久化 RAGFlow 配置和连接状态检查。
- 新增截图提问接口，视觉识别仅生成检索问题，答案仍由知识库资料支撑。
- 数据看板直接读取知识库快照，展示 5 套知识库、692 份文档、1138 个文本分块和 78 个图片分块。
- 修复页面与 Agent 日志中的乱码，移除硬编码重排模型和明文演示密码。

## [0.18.0] - 2026-07-23

### 新增

- 新增 FastAPI 本地演示主入口 `app_fastapi.py`，不再依赖云服务器即可运行学生助手、Agent 日志页、数据看板和连接配置页。
- 新增 `agents_fastapi` 多智能体编排包，包含 Intent、Router、Study Place、Health、Retriever、Reflection 和 Answer 等显式 Agent 节点。
- 新增 SQLite 本地登录、Session、历史对话保存能力，默认演示账号为 `cch125 / admin123`。
- 新增 Agent 可视化日志接口与页面，记录每次提问的节点、耗时、状态、相似度和最终决策。
- 新增 FastAPI 本地运行说明 `docs/FASTAPI_LOCAL_RUN.md` 和一键启动脚本 `scripts/run_fastapi.ps1`。

### 改进

- 学习地点类口语问题会被 Intent Agent 补全为“找学习/自习地点”，优先返回图书馆开放时间等高可信资料。
- 健康类问题进入 Health Agent，不替代诊断、不推荐具体用药，并引导学生补充校区、是否发烧、校医室地址或报销流程等信息。
- Dockerfile 和 Compose 默认启动 FastAPI，Compose 同时支持 `.env` 与 `.env.local`，适合组员本地协作。

### 验证

- 新增 `tests/fastapi_smoke.py`，覆盖学习地点语义识别和健康问题安全边界。

## [0.17.1] - 2026-07-21

### 修复

- Harness 在重写查询后继续校验原问题的关键事项，防止新增的“官方来源”等通用词把无关页面推成高分答案。
- 新增无关官方页面误召回测试，确保虚构问题经过两轮 Loop 后仍然拒答。

## [0.17.0] - 2026-07-21

### 新增

- 引入 LangGraph.js，将输入检查、RAGFlow 检索、质量判断、问题改写、回答与拒答组织为显式状态图。
- 新增 Harness 控制层：校验相似度、内容长度和暨南大学官方来源，最多受控重试两次。
- 第一轮失败后扩展到通知知识库并调整混合检索参数，第二轮启用关键词检索；工具暂时失败也进入有限重试。
- 回答页新增执行轨迹，显示每个节点、查询改写、相似度、耗时与最终决策。
- 数据看板新增智能检索编排流程图。
- 新增 4 个编排自动化测试，覆盖首次通过、重试拒答、工具失败恢复和敏感问题前置拦截。

### 安全

- 账号密码、个人隐私、具体用药、录取保证和未发布信息在检索前直接拒绝。
- 多次检索仍缺少可靠资料时只返回拒答，不使用低质量分块生成答案。

## [0.16.4] - 2026-07-21

### 修复

- Cloudflare 共享通道固定使用 HTTP/2 与 IPv4，避开部分校园网络的 QUIC 和 IPv6 路由故障。
- Windows 自动恢复脚本改用兼容模式的 HTTP GET 健康检查，避免旧版 PowerShell 在 HEAD 请求处卡住。

## [0.16.3] - 2026-07-21

### 修复

- 兼容 RAGFlow 压缩后的服务卡片分块，准确截取直接回答、官方来源和下载附件。
- 检索结果无需依赖本地服务卡片文件，也能显示简洁回答与附件下载按钮。

## [0.16.2] - 2026-07-21

### 修复

- 将核心服务卡片纳入 Vercel API 函数构建产物，线上托管问答可读取本地校验资料。
- 服务卡片目录不可用时自动降级到 RAGFlow 检索，避免整个问答接口失败。

## [0.16.1] - 2026-07-21

### 修复

- Web 助手同步最终调参结果：向量权重改为 `0.9`、相似度阈值改为 `0.2`，并启用 BGE 重排模型。
- 托管模式增加核心服务卡片关键词校验，明确事项不再被高分但无关的文本分块覆盖。
- 回答页直接显示知识库中已确认的附件下载按钮。

## [0.16.0] - 2026-07-21

### 新增

- 学生助手支持管理员托管 RAGFlow，Vercel 配置后所有访问者可直接查询，无需填写地址、API Key 或知识库。
- 数据看板新增实时 RAGFlow 连接状态、知识库数、文档数和分块数，同时保留 GitHub 快照作为离线视图。
- 连接页面在托管模式下自动显示只读知识库清单，不再暴露个人配置表单。
- 新增 Windows 登录后共享服务启动脚本，自动等待 RAGFlow、建立 Cloudflare HTTPS 通道、更新 Vercel 环境变量并重新部署。

### 安全

- 托管模式的 RAGFlow API Key 仅保存在 Vercel 服务端环境变量，不会发送到浏览器或写入 GitHub。
- 托管模式禁止网页上传和项目快照导入，数据维护只能由管理员在本机 RAGFlow 或恢复脚本中完成。
- 个人连接模式继续保留，未配置托管环境时仍可由每位组员连接自己的公网 RAGFlow。

## [0.15.0] - 2026-07-20

### 新增

- 新增 Windows 团队知识库一键恢复脚本，组员从 GitHub 下载项目后可将知识库快照恢复到自己的本地 RAGFlow。
- 默认恢复全部 5 个知识库和 692 条知识库记录；也可只恢复核心服务卡片与第一阶段综合库。
- 自动重建 A/B/C 的 500、800、1200 tokens 分块流水线，分批上传文件并触发解析。
- 新增恢复指南，连接与导入页面增加零预算本地恢复入口。

### 安全与可靠性

- 导入前校验完整知识库快照的 SHA-256，同名文档自动跳过，可安全重复执行。
- RAGFlow API Key 仅保存在当前 PowerShell 进程，脚本结束后立即清除，不写入 GitHub或恢复报告。
- 保留仅供下载的原始附件，不对原快照中没有分块的文档强制解析。
- 明确标注 Vercel 无法访问组员本机 localhost，避免把本地恢复误认为线上共享 RAGFlow。

## [0.14.0] - 2026-07-20

### 新增

- 新增 Next.js 16 全栈 Web 版本，可由 v0 导入并直接部署到 Vercel，包含学生助手、数据看板和连接与导入三个页面。
- 每位组员可在浏览器中配置自己的 RAGFlow HTTPS 地址、API Key、问答知识库和通知补充知识库。
- 新增 Vercel Server API 代理，支持连接验证、知识库读取、文档状态、文件上传、解析触发和可信检索。
- 支持将 GitHub 中 5 套知识库快照分批导入组员选择的 RAGFlow 知识库，同名文件自动跳过。
- Web 助手支持文字与照片输入；照片先由服务器端视觉模型提取检索线索，再查询 RAGFlow。

### 安全与部署

- API Key 默认只保存在浏览器会话中，不写入服务器、日志或 GitHub；用户可主动选择仅在当前浏览器记住。
- 公网代理强制 RAGFlow 使用 HTTPS，并拒绝 localhost、回环地址和内网地址，降低 SSRF 与密钥明文传输风险。
- 查询继续使用相似度阈值和知识库原文摘录；无可靠检索结果时明确拒答。
- 使用当前修复版本 Next.js 16.2.10，并通过 TypeScript、生产构建、Python 单元测试和桌面/移动端页面检查。

## [0.13.0] - 2026-07-20

### 新增

- 将本机 RAGFlow 中 5 个知识库完整导出并纳入 GitHub 版本管理，包含 A/B/C 实验库、核心服务卡片库和第一阶段综合库。
- 快照包含 692 份知识库文档、1,138 个文本分块和 78 个原生图片块，导出错误为 0。
- 新增 `ragflow/export_knowledge_bases.py`，可通过 RAGFlow REST API 重建最新版快照。
- 快照保存知识库配置、文档元数据、完整分块、关键词、问题、位置、`image_id`、文档原件和原生图片。

### 存储与安全

- 以 SHA-256 对 A/B/C 的重复文档和图片去重：692 份文档对应 238 个唯一文件对象，78 个图片块对应 25 个唯一图片对象。
- 快照总大小约 10.5 MB，最大单文件约 0.82 MB，可直接由 GitHub 和每个 Release 的源码压缩包下载。
- 导出器剔除 RAGFlow API Token、模型 API Key、数据库密码、账号、昵称、创建者 ID、反馈日志和聊天记录，并在导出后执行凭据扫描和校验和生成。

## [0.12.0] - 2026-07-19

### 新增

- 新增 RAGFlow 原生图片同步器，将 MinerU 清洗后的图片和表格截图以 `image_base64` 写入 RAGFlow，由 RAGFlow 对象存储生成真实 `image_id`。
- A/B/C 三套实验知识库各写入并验证 26 个原生图片块，共 78 个；读取接口全部返回有效图片。
- 学生助手新增 JPG、PNG、WebP 照片输入，可识别表格、截图和通知照片，再使用识别结果检索知识库。
- 数据看板新增 A/B/C 原生图片同步状态、`image_id` 验证数和知识库直达入口。

### 安全与自动化

- 上传照片只在内存中缩放并发送给视觉模型，不写入本地磁盘；界面提醒学生先遮挡个人信息。
- 视觉模型只提取脱敏后的检索线索，图片内的指令按不可信文字处理；最终回答继续经过 RAGFlow 检索、来源校验和拒答阈值。
- 每日自动更新在多模态文档解析后自动执行原生图片同步；同步过程幂等，已有且有效的 `image_id` 不会重复创建。

### 验证

- 使用真实转专业表格完成照片提问端到端测试，正确命中 `转专业申请表.md`，相似度为 `0.99`。
- 完成桌面端与移动端截图检查、Python 编译检查、RAGFlow 图片读取检查和重复同步检查。

## [0.11.0] - 2026-07-19

### 新增

- 新增 SiliconFlow 视觉模型标注流程，为 MinerU 图片和表格截图生成可检索的视觉描述、可见文字和关键词。
- 26 个视觉单元完成语义标注，助手可按问题返回文字、原始图片、表格截图或附件下载。
- 新增每日 03:00 自动更新守护进程，串联官网采集、清洗、MinerU、视觉标注、质量检查和 RAGFlow 增量同步；错过运行时间会在下次登录后补跑。
- 数据看板新增自动更新状态、阶段耗时、变更数量、下次运行时间和多模态资源预览。

### 改进

- 重新设计学生助手与数据看板 UI，加入统一导航、移动端布局、稳定的搜索工作区和更紧凑的运维信息层级。
- 将视觉描述写入 RAGFlow Markdown 分块，同时通过受控本地媒体路由展示原始图片，不依赖空的 `image_id`。
- 多模态媒体关联加入视觉文字与关键词，提升界面截图和表格类问题的图片召回。
- RAGFlow A/B/C 知识库重新解析 17 份多模态文档，全部成功。

### 发布

- 从本版本开始创建 GitHub Release，协作者可直接下载每个版本的 Source code ZIP/TAR.GZ；旧标签仍保留在 Tags 页面。

## [0.10.0] - 2026-07-19

### 新增

- 建立正式核心召回实验：40 个独立意图扩展为 160 种问法，并按意图隔离训练集与验证集。
- 加入 Rerank 对照实验，正式助手启用 `BAAI/bge-reranker-v2-m3`，验证集 Recall@1、MRR 与无答案拒答率均为 100%。
- 扩展通知类测试集，覆盖竞赛通知、未来结果、私人分数、虚构比赛和天气等边界问题。
- 助手在核心服务卡片未命中时，可严格阈值检索综合知识库并返回带暨南大学官方来源的原文摘要。
- 加入账号密钥、个人隐私、医疗诊断和录取预测四类独立安全规则，不依赖相似度阈值做风险判断。
- 新增多模态质量门禁、链接有效性检查、过期资料清单和每周自动增量更新任务。
- 新增 Docker 部署、健康检查、访问频率限制、请求大小限制和安全响应头。

### 数据与清洗

- 官网采集改为按 15 个数据源均衡分配页数，并保留历史清单中的有效页面，避免单一站点挤占采集额度。
- 清洗阶段过滤栏目首页、部门介绍和非学生事务噪声，同时恢复遗漏但仍有效的教务处文章。
- 当前保留 129 份高价值网页、34 份核心服务卡片和 17 份 MinerU 多模态文档；A/B/C 实验库使用同一套 180 份语料。
- MinerU 后处理升级到 1.1：图片补充同页上下文说明，空表格从检索单元移除；17 份文档质量检查零警告。
- 检查 166 个官网或附件链接，当前未发现失效链接；44 份较旧资料列入人工时效复核清单。
- 学生事务官方资料来源覆盖 20/20 类，核心服务卡片覆盖 17/20 类，二者在看板中分别展示。

### 实验结果

- 核心服务卡片推荐配置：向量权重 `0.90`、全文权重 `0.10`、阈值 `0.20`、Rerank 开启；160 条问法的验证集 Recall@1、MRR 与拒答率均为 100%，正负分数间隔为 `0.2680`。
- 官方通知回退推荐配置：C 库 231 个分块、向量权重 `0.90`、阈值 `0.75`；5 条通知正例与 15 条无答案问题的验证指标均为 100%，正负分数间隔为 `0.0202`。
- 通知库 Rerank 对照同样达到 100%，但未优于基础召回，因此遵循控制变量实验结论，不在通知回退中增加额外延迟。

### 运维

- 已安装 Windows 计划任务 `JNU Student Assistant Weekly Refresh`，每周日 03:00 自动采集、清洗、质检并同步 RAGFlow。
- 新增 `OPERATIONS.md`、`.env.example`、`Dockerfile` 与 `compose.yaml`，密钥只从环境变量或本机配置读取。

## [0.9.0] - 2026-07-19

### 新增

- 新增 30 个可回答问题和 10 个无答案/高风险问题组成的严格检索测试集。
- 新增 RAGFlow API 自动调优脚本，对 A/B/C 分块方案、向量权重和相似度阈值执行粗搜与精搜。
- 使用固定训练集与独立验证集评估 Recall@1/3/5、MRR、拒答率、响应时间和综合目标。
- 看板新增检索参数自动调优结果，并明确区分旧的 8 题初筛和正式严格测试。

### 实验结果

- 推荐实验库 C：142 个分块，向量相似度权重 `0.75`，最低相似度阈值 `0.70`，`top_k=10`。
- 验证集 Recall@1、Recall@3、MRR 和无答案拒答率均为 100%。
- 最低正确相关分数约 `0.7017`，最高无答案分数约 `0.6913`，安全间隔约 `0.0104`；阈值附近必须拒答或复核。
- 账号密码、个人隐私和医疗建议不依赖相似度判断，继续由独立安全规则强制拒答。

## [0.8.0] - 2026-07-19

### 新增

- 新增 MinerU 后的 Python 二次清洗：过滤页码、重复页眉页脚和装饰内容，并保留来源页码与坐标。
- 表格同时输出 HTML 和 JSON，图片与同页相邻文字建立来源关联，17 份附件全部完成结构化处理。
- 新增 RAGFlow A/B/C 数据流水线，分别使用 500/10%、800/10%、1200/15% 的分块与重叠设置。
- 三套实验知识库使用相同的 97 份清洗语料，共完成 291 次流水线解析并生成 291 条 RAGFlow 日志，零失败。
- 新增 8 个固定问题的检索对照测试，输出命中率、首个相关结果排名、MRR、JSON 和 CSV 报告。
- 数据看板新增三套实验知识库、文件列表、真实日志入口、分块数量和检索评测指标。

### 实验结果

- A：181 个分块，8/8 命中，MRR 0.917。
- B：148 个分块，8/8 命中，MRR 0.917。
- C：142 个分块，8/8 命中，MRR 0.938；当前固定问题集上表现略优。

### 变更

- 实验知识库改为绑定 RAGFlow 数据流水线后再上传和解析，不再使用无流水线日志的直接解析路径。

## [0.7.1] - 2026-07-19

### 修复

- MinerU 清洗成功后不再删除原始 PDF、Excel 等附件。
- 原始附件以不解析方式保留在 RAGFlow 文件列表，提供查看和下载通道。
- 检索仍使用 MinerU Markdown，避免原附件和清洗文本产生重复分块。
- 数据看板将原附件标记为“附件留存（不解析）”，不再误显示为处理中。

## [0.7.0] - 2026-07-19

### 新增

- 新增 MinerU 3.4.4 独立 Python 3.12 运行环境，模型与缓存放在 D 盘。
- 新增 PDF、图片、DOCX、PPTX、XLSX 的 OCR、版面、表格和图片多模态清洗流程。
- 新增 SHA-256 增量清洗清单，未变化文件自动跳过，失败与不支持格式明确记录。
- 新增 MinerU Markdown 自动导入 RAGFlow 第一阶段综合知识库。
- MinerU 文档成功入库后自动移除同名原始附件，避免重复检索。
- 数据流程看板新增 MinerU 成功、失败、不支持、图片、结构化 JSON 与入库状态。
- 新增 MinerU 入库检索回归测试，验证清洗文档能够被实际召回。

## [0.6.0] - 2026-07-19

### 新增

- 数据看板新增学生事务类别与责任部门覆盖报告。
- 自动区分“核心卡片”“仅有资料”和“未覆盖”三种状态。
- 自动生成按高、中、低优先级排序的下一轮数据补充清单。
- 历史未回答问题自动归并，并判断是否可能已由当前服务卡片补充。
- 新增 `/api/coverage` JSON 接口和本地 `coverage_report.json` 报告。

## [0.5.0] - 2026-07-19

### 新增

- 问答页直接显示最多 3 个达到可信阈值的相关结果，不再把候选结果只藏在匹配片段中。
- 每个相关结果独立提供官方说明和附件下载入口。
- 回答标题显示本次找到的可信结果数量。

## [0.4.1] - 2026-07-19

### 修正

- 禁止从多条目栏目页误收其他事项的附件。
- 丝路奖学金旧附件返回 403 时只提供官方栏目入口，不再显示失效下载按钮。

## [0.4.0] - 2026-07-19

### 新增

- 服务卡片生成阶段自动提取暨南大学官方页面中的 PDF、Word、Excel 和压缩包附件直链。
- 助手回答优先提供“直接下载”按钮，同时保留“查看官方说明”入口。
- 通知、办事指南等没有附件的内容只显示官方说明，不会误标为可下载文档。

### 修正

- 提高标题和材料关键词精确命中的优先级，避免下载问题返回语义相近但错误的文档。
- 对需要统一身份认证、在线生成或已失效的附件明确提示，不提供不可用下载按钮。

## [0.3.0] - 2026-07-19

### 新增

- 建立正式版本管理规则，加入 `VERSION`、更新日志和发布检查脚本。
- 数据流程看板显示 RAGFlow 各知识库的文档、分块及解析成功、处理中、失败数量。
- 看板明确区分“知识库文档解析状态”和“RAGFlow 数据管道日志”，避免把空日志误认为空知识库。

### 数据状态

- 核心服务卡片知识库：34 个文档，37 个分块。
- 第一阶段综合知识库：已补充第二阶段学生事务内容，覆盖本科休学、复学、退学、研究生服务、就业手续和公费医疗等事项。

## [0.2.0] - 2026-07-19

### 新增

- 扩大暨南大学学生事务官网采集范围。
- 新增研究生、就业、公费医疗、图书馆、网络服务和校园生活等数据源及分类。
- 核心服务卡片扩展到 34 项。

## [0.1.0] - 2026-07-19

### 新增

- 完成暨南大学学生助手第一阶段原型。
- 实现官网采集、数据清洗、服务卡片生成、RAGFlow 导入和本地问答页面。
- 加入低置信度拒答机制和未命中问题记录。
